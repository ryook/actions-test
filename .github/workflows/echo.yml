name: echo

on:
  workflow_call:
    inputs:
      text:
        description: text
        type: string


jobs:
  echo:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v3
      - name: show input
        run: |
          cat ./README.md

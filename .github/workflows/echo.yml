name: echo

on:
  workflow_call:
    inputs:
      text:
        description: text
        type: string


jobs:
  echo:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v3
      - run: git status
      - run: git branch
